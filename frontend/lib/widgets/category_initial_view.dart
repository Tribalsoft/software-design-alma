import 'package:flutter/material.dart';

class CategoryInitialView extends StatelessWidget {
  const CategoryInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Esperando acción..."),
    );
  }
}
